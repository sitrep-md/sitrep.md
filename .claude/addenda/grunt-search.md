# grunt-search addendum (sitrep-www)

- Searches may cross into the local reference repo (the SEO reference implementation) and the sitrep product repo when the question is about a pattern to port or product positioning. Those are other repos: read only, never suggest edits to them from here.
- `node_modules/`, `dist/` and `.astro/` are noise. Exclude them unless the question is explicitly about a dependency or build output.
